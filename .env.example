# Doc-Contract Go API base (cookie sessions).
# Backend must set FRONTEND_ORIGIN to this Vite origin (default http://localhost:5173)
# so verification / password-reset emails open the SPA.
# SMTP credentials are configured only on the backend (.env EMAIL_PROVIDER=smtp) — never here.
VITE_API_BASE_URL=http://localhost:8080/api/v1
